---
import type { SiteContent } from '../content/types';
interface Props { t: SiteContent }
const { t } = Astro.props;
---

<section class="surface-deep border-y border-ink/[0.07]">
  <div class="container-page section">
    <div class="container-text text-center" data-reveal>
      <h2 class="h-rule rule-center">{t.whatIs.h2}</h2>
      <p class="mt-7 text-[1.1rem]">{t.whatIs.p1}</p>
    </div>

    <!-- The three channels — the dot device doing its one job -->
    <ul class="mt-10 flex flex-wrap justify-center gap-4" data-reveal>
      {t.whatIs.channels.map((c) => (
        <li
          class="card flex items-center gap-3 !rounded-full px-7 py-3.5 text-[1.15rem] font-semibold"
        >
          <span class="h-2.5 w-2.5 shrink-0 rounded-full bg-brass-deep" aria-hidden="true"></span>
          {c}
        </li>
      ))}
    </ul>

    <p class="container-text mt-10 text-center text-[1.15rem] font-medium" data-reveal>
      {t.whatIs.p2}
    </p>
  </div>
</section>
